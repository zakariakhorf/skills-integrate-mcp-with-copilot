# Issue: Add tests and CI

**Summary**
Add unit tests to cover core functionality and a GitHub Actions workflow to run tests and linters on push/PR.

**Acceptance criteria**
- Add pytest and basic test coverage for endpoints (auth, signup, DB-backed flow).
- Add a `.github/workflows/ci.yml` that runs tests and linters.
- Publish code coverage report (optional: Codecov integration).

**Labels:** enhancement, tests, priority:medium
